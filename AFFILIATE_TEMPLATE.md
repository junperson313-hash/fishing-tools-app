# 便利ツール+アフィリエイトサイト テンプレート

「釣り便利ツール」で確立した、無料の計算/早見表ツールを軸にしたアフィリエイトサイトの設計パターン。別ジャンル(例: キャンプ道具、DIY計算、家計計算など)でも同じ型で新規サイトを作るときのチェックリストとして使う。

Claude Codeでこの型の新規サイトを作るときは、着手前にこのファイルを読み、以下の各項目を最初から組み込んだ状態で設計すること。

## 1. 全体コンセプト

「検索流入 → 無料ツール利用 → 必要な商品を自然に購入」という導線を前提に設計する。

- ツールは「入力して計算する」より「早見表から探す」の方が、数値が離散的(号数・サイズなど決まった刻みでしか商品が存在しない)な領域では実態に合っていることが多い。ユーザーが実際に細かい数値を気にするか、実店舗/通販でどう商品が売られているかを先に確認してから、入力式にするか早見表式にするか決める。
- 数値・換算値は必ずWeb検索で複数の独立したソースと突き合わせてから公開する。特に「業界標準がある値(例: 1号=3.75g)」と「メーカー・製品でバラつく値(例: PEラインの号数と強度)」を区別し、後者は数値を断定せずレンジで示し、根拠と限界をページ内に明記する。裏取りしていない数値を公開しないことが最優先。

## 2. アフィリエイトコンポーネント設計

- IDやURL生成ロジックは`src/lib/affiliate.ts`のような1ファイルに集約し、コンポーネントからは直接URLを組み立てない。将来的に楽天以外(Amazon等)を追加する場合も、同じファイルに並行するクライアントを足す形にする。
- 商品検索は公式APIを優先する(例: 楽天市場商品検索API)。API利用時は必ず実際にcurl等で叩いて認証方式・レスポンス形式を確認する。**公式ドキュメントの要約だけを信じない** — 実際に試したら仕様が古かった/フィールド名が違った、ということが起こりうる。
- APIの認証情報(アプリID、アクセスキー等)はサーバー専用の環境変数に置き、`NEXT_PUBLIC_`を付けない。公開してよいのはアフィリエイトIDのように「知られても実害がない」値だけ。
- API未設定・エラー時は、素の検索リンクにフォールバックし、ページが壊れないようにする(`src/lib/rakuten-api.ts`の`searchRakutenItems`パターン)。
- 商品表示ブロックは`AffiliateBlock`のような共通コンポーネントに統一し、`category`(商品カテゴリ)・`reason`(推薦理由、任意)・`keyword`(検索/API用キーワード)を渡すだけで使えるようにする。
- 個別の値(号数・サイズ等)に連動した購入導線を作る場合は、`SelectableTable`のように「行選択→その値の検索リンク表示」という軽量なクライアントコンポーネントにする。数値入力の再現は避け、早見表の行タップで完結させる。
- 全ての商品リンクにPR表記を明記する(景品表示法のステマ規制対応)。文言例: 「◯◯市場の商品情報です。価格は変動することがあります。PR/広告リンクを含みます。」
- 商品リンクのクリックは`AffiliateLink`のような薄いクライアントラッパーで計測イベントを発火できるようにしておく(GA4未接続でも壊れないガード付き)。

## 3. ツールページテンプレート

共通構成: パンくず → H1 → 説明 → ツール本体(早見表/計算) → 結果連動の購入導線 → 使い方 → (注意書き、必要な場合) → 詳しい解説 → FAQ → 関連ツール(→ 関連ガイドへのリンク、あれば)

これを`ToolPageLayout`のような1つのレイアウトコンポーネントに固定し、新しいツールを追加するときはデータ(早見表・FAQ文言等)を用意して呼び出すだけにする。ページごとに数百行コピペする状態を避ける。

## 4. ロングテール購入導線ページ(Q&Aガイド)

「PE1号に合うリーダーは何号？」のような、検索意図が明確で購入意欲の高い一問一答ページは、`/guides/[slug]`のような専用ルートにまとめる。

- 型は「回答→根拠となる早見表/目安→関連ツールへのリンク→購入導線」の4点に固定し、SEO目的の水増し文章を入れない。
- `QAPage`または`FAQPage`の構造化データを付ける。
- 内容のある質問だけを公開し、量産しない。公開予定のものはコード内にコメント・データとしてロードマップを残す。

## 5. SEO

各ページで以下を必ず設定する:
- `title` / `description` / `alternates.canonical` / `openGraph`(ページごとの`metadata`エクスポート)
- `BreadcrumbList`構造化データ(`Breadcrumb`コンポーネント)
- `FAQPage`または`QAPage`構造化データ(該当する場合)
- トップページに`WebSite`構造化データ
- 動的`opengraph-image`(Next.jsの`opengraph-image.tsx`規約)
- `sitemap.ts` / `robots.ts`は、ツール一覧・ガイド一覧の配列(例: `TOOLS`, `GUIDES`)から自動生成する。新規ページ追加時にこの配列に足すだけでサイトマップ・内部リンク(`RelatedTools`)の両方に自動反映されるようにする。
- Google Search Console用の`verification.google`をmetadataに設定する。

## 6. Analytics

- `src/lib/analytics.ts`に`trackEvent(name, params)`のような薄いラッパーを用意し、`window.gtag`が無くても安全に呼べるようにする。
- GA4のスクリプトタグは`layout.tsx`で`NEXT_PUBLIC_GA_ID`が設定されている場合のみ読み込む(未契約でもビルド・デプロイが壊れないようにする)。

## 7. モバイル最適化

- スマホでの利用が前提。フォントサイズ・タップ領域・テーブルの横スクロール対応を必ず確認する。
- 画面幅390px程度でのスクリーンショット確認をリリース前に必ず行う(Playwright等)。

## 8. 必須の固定ページ

- トップページ(ツール一覧への導線)
- このサイトについて(運営目的・数値の考え方・広告についての説明)
- プライバシーポリシー(広告配信・アクセス解析・アフィリエイト・免責事項)

## 9. デプロイ・運用

- 無料枠(Vercel Hobby等)で完結する構成にする。
- 環境変数(APIキー、アフィリエイトID等)はデプロイ先の環境変数機能で管理し、コードにベタ書きしない。
- 既存のアフィリエイトID・APIキーは、明示的な指示がない限り変更しない。
